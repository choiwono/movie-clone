import React, { ReactFragment, Fragment } from 'react';
// popular, trending
interface Props {

}

const About: React.FC<Props> = props => {
    const { } = props;

    return (
        <Fragment>
            어바웃
        </Fragment>
    );
};

export default About;